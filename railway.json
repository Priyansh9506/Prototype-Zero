{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "nixpacksPlan": {
            "providers": [
                "python"
            ]
        },
        "buildCommand": "pip install -r requirements-deploy.txt"
    },
    "deploy": {
        "startCommand": "python -c \"from api.database import engine, Base; Base.metadata.create_all(bind=engine); print('DB tables ready')\" && uvicorn api.main:app --host 0.0.0.0 --port $PORT",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}